# TaskFlow Mobile (Android)

Мобильное приложение для управления проектами на Android (Kotlin + Jetpack Compose).

## 🛠️ Технологии

- **Kotlin** — язык программирования
- **Jetpack Compose** — декларативный UI
- **Material 3** — дизайн-система
- **Retrofit 2** — HTTP-клиент
- **Gson** — сериализация JSON
- **Coroutines** — асинхронность
- **Android SDK 35** — целевая версия
- **Min SDK 24** — Android 7.0+

## 📱 Требования

- Android Studio Hedgehog (2023.1.1) или новее
- Android SDK 35
- Kotlin 1.9+
- Gradle 8.x

## 🚀 Запуск

### В Android Studio

1. Откройте папку `project11b-mobile-svobodnaya` в Android Studio
2. Дождитесь синхронизации Gradle
3. Настройте URL API сервера (см. раздел Конфигурация)
4. Запустите на эмуляторе или устройстве

### Через командную строку

```bash
# Сборка debug APK
./gradlew assembleDebug

# Установка на подключённое устройство
./gradlew installDebug

# Запуск тестов
./gradlew test
```

## 📱 Экраны

| Экран | Описание |
|-------|----------|
| LoginScreen | Вход в аккаунт |
| RegisterScreen | Регистрация нового пользователя |
| ProjectsScreen | Список проектов пользователя |
| ProjectDetailScreen | Детали проекта с задачами |

## 🎨 Функциональность

### Аутентификация
- Регистрация нового пользователя
- Вход по логину/паролю
- Хранение токена для автоматической авторизации
- Выход из аккаунта

### Проекты
- Просмотр списка проектов
- Создание нового проекта
- Просмотр деталей проекта
- Управление участниками

### Задачи
- Просмотр задач проекта
- Создание новых задач
- Редактирование задач (статус, сложность, ответственный)
- Удаление задач
- Переключение между режимами отображения:
  - 📋 Список
  - 📊 Kanban-доска

### UI/UX
- Material 3 дизайн
- Оранжевая цветовая схема
- Поддержка тёмной темы
- Адаптивный интерфейс

## 📁 Структура проекта

```
project11b-mobile-svobodnaya/
├── app/
│   ├── src/main/
│   │   ├── java/com/example/huiban/
│   │   │   ├── MainActivity.kt           # Главная Activity
│   │   │   ├── data/
│   │   │   │   ├── api/
│   │   │   │   │   ├── ApiService.kt      # Retrofit API интерфейс
│   │   │   │   │   └── RetrofitClient.kt  # Настройка Retrofit
│   │   │   │   ├── model/
│   │   │   │   │   └── Models.kt          # Модели данных
│   │   │   │   └── repository/
│   │   │   │       ├── AuthRepository.kt  # Репозиторий авторизации
│   │   │   │       ├── ProjectRepository.kt
│   │   │   │       └── TaskRepository.kt
│   │   │   └── ui/
│   │   │       ├── screens/
│   │   │       │   ├── LoginScreen.kt
│   │   │       │   ├── RegisterScreen.kt
│   │   │       │   ├── ProjectsScreen.kt
│   │   │       │   └── ProjectDetailScreen.kt
│   │   │       └── theme/
│   │   │           ├── Color.kt           # Цветовая палитра
│   │   │           ├── Theme.kt           # Настройка темы
│   │   │           └── Type.kt            # Типографика
│   │   ├── res/                           # Ресурсы
│   │   └── AndroidManifest.xml
│   ├── build.gradle.kts
│   └── proguard-rules.pro
├── gradle/
│   ├── libs.versions.toml                 # Версии зависимостей
│   └── wrapper/
├── build.gradle.kts
├── settings.gradle.kts
└── README.md
```

## ⚙️ Конфигурация API

URL бэкенда настраивается в файле `RetrofitClient.kt`:

```kotlin
private const val BASE_URL = "http://10.0.2.2:8000/"  // Для эмулятора (localhost)
// или
private const val BASE_URL = "https://your-api-domain.com/"  // Для продакшена
```

> **Примечание:** `10.0.2.2` — это специальный IP для доступа к localhost хост-машины из Android эмулятора.

Для физического устройства в локальной сети используйте IP-адрес компьютера.

## 🔧 Зависимости

```kotlin
// Compose
implementation("androidx.activity:activity-compose")
implementation("androidx.compose.material3:material3")
implementation("androidx.compose.material:material-icons-extended")

// Retrofit
implementation("com.squareup.retrofit2:retrofit:2.9.0")
implementation("com.squareup.retrofit2:converter-gson:2.9.0")
implementation("com.squareup.okhttp3:logging-interceptor:4.12.0")

// Coroutines
implementation("org.jetbrains.kotlinx:kotlinx-coroutines-android:1.7.3")
```

## 🧪 Тестирование

```bash
# Unit тесты
./gradlew test

# Instrumented тесты
./gradlew connectedAndroidTest
```

## 📦 Сборка релиза

```bash
# Сборка release APK (требуется signing config)
./gradlew assembleRelease
```

APK будет в `app/build/outputs/apk/release/`

## 🎨 Тема и стилизация

Приложение использует Material 3 с кастомной оранжевой темой:

```kotlin
// Color.kt
val Orange = Color(0xFFFF6B00)
val OrangeLight = Color(0xFFFF8C42)
val OrangeDark = Color(0xFFE55B00)
```

Поддерживается как светлая, так и тёмная тема.
